# 泰山派离线语音助手

板上部署路径为 `/opt/eda-aibox`。`ai.service` 以 `yang` 用户运行，依赖本机
`ollama.service`。Python 运行环境为 `/opt/eda-aibox/venv`，仅访问
`127.0.0.1:11434` 上的 Ollama；模型均保存在板上，不需要持续联网。

## 当前模型

| 功能 | 模型 | 执行设备 |
| --- | --- | --- |
| 识别 | RK3566 版 5 秒 SenseVoice，中英等多语 | RKNPU |
| 对话 | Qwen3 1.7B（默认）或 0.6B，4 位量化，关闭思考模式 | CPU |
| 合成 | Piper 小雅 VITS，22.05 kHz | CPU |

`/opt/eda-aibox/asr_backend` 设置为 `sensevoice`。语音按静音或最长 4.5 秒分段，
避免 SenseVoice 固定输入长度截断。也可将该文件内容改为 `cpu` 后重启服务，
使用流式 Zipformer ONNX CPU 备用识别。

RK3566 版流式 Zipformer RKNN 在此板首次推理报
`failed to submit, op id: 42, Split:/Gather_64_2sl_2sp`，因此未启用。
该报错与 [sherpa-onnx #2583](https://github.com/k2-fsa/sherpa-onnx/issues/2583)
相同。SenseVoice 已连续完成 10 次 NPU 测试，监测到 NPU 负载。
4.5 秒样本的 NPU 推理用时约 2.7–2.8 秒，最高观测负载 77%；
CPU 备用 Zipformer 的两段样本实时系数约 0.44。旧 Qwen3 0.6B 在模型常驻后
一次短回答的解码速度约 9 token/s；当前 1.7B 在对比题中约 3 token/s。
以上只是测试音频的性能记录，
在自带中文样本上将“开放”识别成“开饭”，以文档转写为参考，去掉标点后
字符错误率为 1/13（7.7%）。这一个样本不能代表真实环境的错误率，
麦克风语料仍需单独评测。

### NPU 的使用边界

目前 NPU 已用于 SenseVoice 语音识别，回答阶段的 Qwen3 和小雅仍在 CPU。
[Rockchip RKLLM](https://github.com/airockchip/rknn-llm/blob/main/README.md)
当前支持平台列出 RK3588、RK3576、RK3562、RV1126B，未列出 RK3566，
因此不能把 Qwen3 在本板 NPU 运行当作现成升级选项。
[RKNN Model Zoo](https://github.com/airockchip/rknn_model_zoo/blob/main/README.md)
虽列出 RK3566 TTS，但所提供的 MMS 模型是英文；不能代替已试听确认
清楚的中文小雅声音。小雅当前 ONNX 模型具有动态音素输入和动态音频输出，
2682 个节点；即使静态指定 64 个输入音素，图形状推断仍得不到固定输出
长度。Rockchip 的 [MMS-TTS 导出说明](https://github.com/airockchip/rknn_model_zoo/blob/main/examples/mms_tts/export_onnx.md)
也要求将 VITS 拆分以处理动态形状。若研究中文 NPU TTS，应在独立环境
完成模型拆分与 RKNN 转换，再在本板比较中文清晰度、首段延迟、CPU/NPU
负载；只有实测优于当前小雅后再切换服务。

`/opt/eda-aibox/tts_backend` 设置为 `xiao_ya`。4.2 秒语音的板上合成时间
约 4.0 秒，原 AISHELL3 的同一句为 2.1 秒。小雅声音经用户试听确认更清楚；
AISHELL3 留作加载失败时的备用。

Qwen3 通过 Ollama 流式输出。程序收到完整句子就送入有界队列；较长句子
也可在自然逗号处提前切分。生成文本、合成下一段和播放上一段分别运行，
OLED 随正在朗读的段落更新。上一版板上测试中，第一段开播比本轮
文本生成结束早约 1.2 秒；随后用板上测试音频验证
NPU 识别、Qwen3 回答、小雅合成和扬声器播放均成功。

2026-09-24 的性能调优：四核 RK3566 上，Qwen3 默认线程设置稳定约
9.8–10.0 token/s，手动设 2/3/4 线程分别约 6.7/8.8/10.0 token/s，
因此保留默认值。小雅合成 4.4 秒的固定语音，1/2/3/4 线程耗时中位数
分别约 6.93/4.18/3.54/3.27 秒；与 Qwen 并行时 4 线程未显示稳定
优势，因此继续用 3 线程。通过提前在自然逗号处分段、将合成和播放
并行，缩短多句回答的等待；声音模型和播放语速不变。新流水线已用
真实 Qwen 流输出验证生成与合成重叠、合成与播放重叠，并通过真实
NPU→Qwen→小雅→扬声器/OLED 整链测试。
1.15 倍语速的同句样本合成 2.87 秒、音长 3.53 秒，原速分别为
3.40 秒和 4.10 秒；用户试听认为倍速太快或不够清楚，因此保留原速。

### 回答质量升级（2026-09-25）

原 0.6B 模型在实际语音日志中对“讲个故事”无故拒答，排查 LED 亮度时
只给笼统建议，并把天空呈蓝色错误地归因于水分子。将回答上限从 160
提高到 256 token、加强提示词后仍出现明显事实错误，因此改用
`qwen3:1.7b-q4_K_M`。新提示词要求直接回答、按顺序给可执行排查步骤、
保留必要上下文，并且不重复上一轮拒答；对话历史最多保留两轮。
Ollama 继续按句流式输出到 TTS，不等待全文生成结束。朗读前清除偶发的
Markdown 标记。

在同一板上的三道对比题中，1.7B 能直接讲故事，并在 LED 问题里指出
PWM 频率和占空比，但天空颜色题仍把蓝光波长长短说反，不能保证事实
准确。首次加载后的故事用时 46.81 秒、83 token；常驻后的 LED 与天空
题分别用时 49.44 秒／147 token、26.33 秒／80 token。4B Instruct
在短题上超过 90 秒仍未完成，Qwen3.5 0.8B 在故事和 LED 题均达到
256 token 上限并产生冗长或错误内容，因此没有设为默认模型。
模型包可由 [Ollama Qwen3 模型列表](https://ollama.com/library/qwen3/tags)核对。

服务设有 `EDA_LLM_MODEL=qwen3:1.7b-q4_K_M`，需要临时回退时可改为
`qwen3:0.6b` 后重启 `ai`。`EDA_MAX_TOKENS` 默认 256，允许 80–512；
复杂问题可按需调高，但完整朗读时间会增加。解释温湿度或灯控故障的
提问交给 Qwen 回答，实际读取温湿度、开关灯和调亮度仍由本地程序执行。

## 操作

```sh
systemctl status ai ollama
journalctl -u ai -f
cat /sys/kernel/debug/rknpu/load
printf cpu | sudo tee /opt/eda-aibox/asr_backend
sudo systemctl restart ai
```

板上音频设备目前设置为 `plughw:1,0`，可通过 `EDA_CAPTURE_DEVICE` 和
`EDA_PLAYBACK_DEVICE` 覆盖。服务启动时会设置主麦克风、SPK 扬声器路径
和 DAC 音量 250/255。合成音频会做峰值限制后转换到 48 kHz 播放。
OLED 为 SSD1306 128×64，默认使用 I2C 2 总线的 `0x3c` 地址；
没有接入时继续运行语音服务。SenseVoice 录音会先采样约 0.5 秒
背景音，再以背景音量的 2.5 倍作为触发门限，并持续更新背景估计。
`EDA_VAD_RMS` 为最低门限，默认 `0.012`；`EDA_VAD_MULTIPLIER`
调整背景音量倍率，默认 `2.5`。两者均可在服务配置中覆盖。

唤醒采用识别后的文字判断：只有同一句话出现“小嘉”“小佳”及常见
同音写法，才向 Qwen 发问并朗读；未包含称呼的语句保持安静。单独呼叫
名字时简短回答“我在，请说”。“小家伙”等常见词不会触发。它不是
独立的低功耗唤醒模型，因此每句语音仍会先经过 NPU 识别。
OLED 在待机时显示眨眼、轻微呼吸表情，约一分钟无变化后切为困倦；
识别、思考、朗读和故障时分别显示聆听、思考、说话/开心/好奇和难过
表情，下方保留中文文字。动画在独立线程以约 4 帧/秒刷新，不阻塞
录音与播放。2026-09-24 板上自动检查通过唤醒过滤、无唤醒不发声、
单独唤醒应答和表情帧变化；真实麦克风的两句对照仍需人工确认。

## AHT10 温湿度

I2C 2 总线上除 OLED 的 `0x3c` 外检测到 AHT10 的 `0x38`。
运行 `/opt/eda-aibox/venv/bin/python /opt/eda-aibox/aht10_read.py --count 3`
可读取摄氏温度、相对湿度、状态字和原始数据。2026-09-24 连续三次读数
为 36.62°C，湿度 79.57%、79.38%、79.28%，状态 `0x1c`，校准位
已置位且忙标志已清除。这反映传感器所在位置的读数，不一定等于房间
平均环境温湿度。语音询问天气、温度或湿度时，在唤醒词通过后立即
读取 AHT10 并朗读当前板子附近温湿度；回答会注明这不是室外天气或
天气预报。读数失败时会说明无法读取，不会让大模型猜测。可用
`EDA_AHT_BUS` 和 `EDA_AHT_ADDRESS` 更改总线及地址（默认 2 和
`0x38`）。

2026-09-25 复测时地址仍可扫描到，但 6 字节数据读取报
`OSError: [Errno 6] No such device or address`。当前语音天气请求会如实
告知读数不可用；需单独检查传感器接线、供电和 I2C 稳定性。

## GPIO 按键和 OLED 页面

GPIO3_A1、GPIO3_A2、GPIO3_A3 分别为左、中、右键，在 Linux 中对应
`/dev/gpiochip3` 的 1、2、3 号线（GPIO 97、98、99）。按键按下接地，
释放为高电平。服务通过 `gpiomon` 的内核下降沿事件接收按键，不循环
轮询 GPIO；40 ms 硬件去抖加 150 ms 软件间隔。udev 规则
`90-eda-aibox-gpio.rules` 将此设备授权给 `yang` 用户。

左／右键在主页面间切换：表情页、天气页、设置页。在设置页，左／右键
选择“显示／对话／灯／提示词／模型／返回”，中键切换或确认；设置项超过四行时
OLED 随选择滚动。显示模式依次是纯表情、
表情＋文字、纯文字；对话模式依次是唤醒、连续、按键。设置保存在
`/var/lib/eda-aibox/settings.json`，重启后保持。天气页每 30 秒刷新
一次 AHT10，按中键可立即刷新。静态画面变化时才更新 OLED。

“提示词”默认开启。关闭后，发给 Ollama 的对话消息不再包含小嘉的系统
提示词；模型、思考模式、对话历史及本地语音指令处理保持当前配置。
切换立即对下一次提问生效，并随其他设置保存。
“模型”可选 0.6B 或 1.7B，默认 1.7B；所选模型保存到同一设置文件，
下一次提问使用新模型，切换时清空旧模型的对话历史。首次使用某个模型
可能需要等待 Ollama 加载。回答期间按中键会停止正在播放的音频，
取消后续分句的合成和朗读，并丢弃本次未完成回答；随后返回聆听状态。

唤醒模式保持原有的“小嘉／小佳”门限。连续模式在检测到语音后直接
回答，无需先说唤醒词，也没有 30 秒会话窗口；要停止自动应答，可在
设置页切回唤醒或按键模式。按键模式在表情页或天气页按中键后录制下一句，
该句不必含唤醒词；无语音则约 20 秒后返回待机。表情页在其他对话
模式按中键会进入设置页。

## GPIO0_B7 灯控

GPIO0_B7 是 GPIO 15，同时复用为 PWM0_M0。启动覆盖层
`/boot/overlay-user/eda-led-pwm0.dtbo` 只启用 PWM0；`/boot/armbianEnv.txt`
中的 `user_overlays=eda-led-pwm0` 使其重启后生效。服务启动前运行
`setup_led_pwm.py`，将 PWM0 导出并授权给 `yang`。亮度由内核以 1 kHz
PWM 输出，无需在 Python 中循环翻转引脚。服务进程通过
`/sys/devices/platform/fdd70000.pwm/pwm/pwmchip*/pwm0` 写入占空比。

语音示例：“小嘉，开灯”“小嘉，关灯”“小嘉，亮度调到百分之五十”
“小嘉，调亮一点”“小嘉，调暗一点”。按键设置页的“灯”一项也可
依次选择 25%、50%、75%、100%、关闭。亮度和开关状态保存在
`/var/lib/eda-aibox/led.json`。如外接 LED 采用低电平点亮，可在服务
配置中设置 `EDA_LED_ACTIVE_LOW=1` 并重启服务。

麦克风、扬声器和 OLED 已完成单轮真实对话测试。2026-09-24 在 Wi-Fi
关闭且 `nmcli radio wifi` 显示 `disabled` 时，使用板上自带中文测试音频
完成 SenseVoice NPU 识别 → 本地 Qwen 回答 → 小雅合成及扬声器播放 → OLED
显示，`offline-smoke.service` 正常退出（39.4 秒墙钟时间，其中识别 2.81 秒，
回答及播放 13.74 秒）。这是测试音频整链验证，不能代替断网时真实麦克风
输入的验收。切换小雅后，用户已确认实时语音回答清楚、OLED 显示正常。
重启后 `ai.service` 和 `ollama.service` 均自动启动，日志显示 NPU 识别、
小雅 TTS 与 Qwen3 准备就绪。
断网时真实麦克风输入、多轮对话稳定性和噪声门限仍需继续验收。
常见技术缩写在朗读前转成中文读法，其他英文词的朗读效果需实测。
